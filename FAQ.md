# failed to inject CDI devices: unresolvable CDI devices

/var/lib/vastai_kaalia/latest/swap_gpu.py --skip_swap
 --new_uuid (required, the replacement GPU), 
 --old_uuid (optional, the specific dead GPU to replace), 
 --skip_swap (just re-report GPU list without changing anything).

